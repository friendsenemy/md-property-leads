services:
  - type: web
    name: md-property-leads
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 1 --threads 2 "app:create_app()"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
